import 'package:flutter/cupertino.dart';

class SearchOnTrackPlayingWidget extends StatelessWidget {
  const SearchOnTrackPlayingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}